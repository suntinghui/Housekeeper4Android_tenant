<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 设置背景色 -->

    <item android:id="@android:id/background">

        <shape>

            <gradient
                android:endColor="#E1F2FA"
                android:startColor="#E1F2FA" />
        </shape>
    </item>

    <!-- 设置进度条颜色 -->

    <item android:id="@android:id/progress">

        <clip>

            <shape>

                <gradient
                    android:endColor="@color/blueme"
                    android:startColor="@color/blueme" />
            </shape>
        </clip>
    </item>

</layer-list>