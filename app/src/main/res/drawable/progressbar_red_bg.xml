<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 设置背景色 -->

    <item android:id="@android:id/background">

        <shape>

            <gradient
                android:endColor="#FDD5D6"
                android:startColor="#FDD5D6" />
        </shape>
    </item>

    <!-- 设置进度条颜色 -->

    <item android:id="@android:id/progress">

        <clip>

            <shape>

                <gradient
                    android:endColor="@color/redme"
                    android:startColor="@color/redme" />
            </shape>
        </clip>
    </item>

</layer-list>